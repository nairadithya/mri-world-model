torch>=2.2
monai>=1.3
nibabel>=5.2
SimpleITK>=2.4
peft>=0.8
transformers<5  # peft 0.20 imports Bloom classes removed in transformers 5
# NOTE: do NOT install torchvision (optional dep of transformers/monai);
# a version-skewed torchvision breaks peft import with torchvision::nms error
einops>=0.7
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
wandb>=0.15
hd-bet
safetensors>=0.4
tqdm>=4.65
