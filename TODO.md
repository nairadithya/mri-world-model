# TODO — ASTRA research program

Goal: establish a fair response-assessment benchmark, then build a genuinely prospective, lesion-aware forecasting model that generalizes across sites.

**Rule:** no new broad JEPA training sweep until the validity gates are complete.

## P0 — Make the evaluation trustworthy

- [x] Define separate task manifests for:
  - [x] observed-scan response assessment (`x_t + x_{t+1} -> RANO_{t+1}`)
  - [x] past-only forecasting (`H_t -> future outcome`)
  - [x] future anatomy forecasting (`H_t -> future lesion state`)
  - [x] surprise/change detection (`prediction + observed x_{t+1}`)
- [x] Add immutable row IDs: patient, source visit, target visit, target type, label version.
- [x] Assert that every method uses the same rows, labels, exclusions, and information cutoff.
- [x] Separate response labels, treatment labels, operative events, and missing labels.
- [x] Measure all rows currently mapped to default SD or excluded as invalid. See `info/p0_audit.md`.
- [x] Remove eventual survival from new prospective dataset inputs.
- [x] Rebuild existing caches with the survival-free schema; current schema-2 LUMIERE and SAILOR caches are recorded in `info/p0_refresh.md`.
- [ ] **BLOCKED — Verify SAILOR RANO code mapping independently and report codebook sensitivity; blocked by lack of an independent expert reference/data-owner confirmation.**
- [ ] **BLOCKED — Reconcile RANO/RANO-2.0 definitions and surgery/radiotherapy timing; blocked by missing aligned radiotherapy-completion/confirmation metadata.**
- [x] Freeze the historical `final` split as development evidence; stop treating it as untouched.
- [ ] **BLOCKED — Reserve/run a genuinely new external or hidden test cohort; blocked by access/challenge rules and no locked new cohort assignment.**

## P0 — Repair metrics and statistical protocol

- [x] Replace `auc_mann_whitney` with a validated implementation.
- [x] Add tests for perfect, reversed, interleaved, tied, empty, and single-class scores.
- [x] Re-run all surprise, persistence-error, lead-time, and site-discrimination analyses; refreshed results are recorded in `info/p0_refresh.md` and `info/p0_audit.md`.
- [x] Preserve logits/scores in `ReadoutEvaluator` for AUROC/AUPRC/calibration.
- [x] Bootstrap the metric actually selected, not always macro-F1.
- [x] Add AUPRC, calibration, Brier/log loss, sensitivity, specificity, and prevalence metrics to the harness.
- [x] Report pooled and patient-uniform aggregation across the complete refreshed evaluation. The reproducible scorecard is `outputs/p0_scorecard.json`, generated by `scripts/p0_scorecard.py`.
- [x] Report paired patient-cluster CIs and readout training-seed/width variation for the refreshed results; the scorecard uses 10,000 patient resamples and a 3-seed × 3-width sensitivity grid.
- [x] Make inner ridge CV patient-grouped when used through `ReadoutEvaluator`.
- [x] Freeze the readout policy for development comparisons: MLP, hidden width 256, seed 42, no feature standardization; sensitivity results are retained in `outputs/p0_scorecard.json`.
- [x] Add cache schema/provenance validation and make unified CLI evaluation reject legacy caches.

**P0 status:** all locally actionable implementation work is complete. The current-schema scorecard and readout policy are frozen. The remaining P0 items are explicitly `BLOCKED` above and require independent label/timing confirmation or a new external/hidden cohort.

**Exit condition:** a row-matched evaluation table can be regenerated from scratch, with no ambiguous labels or invalid AUCs.

## P1 — Establish the real prospective floor

- [x] Implement training-majority and per-class prevalence baselines.
- [x] Implement last-observed-RANO baseline.
- [x] Implement a smoothed patient-level RANO transition model.
- [x] Implement demographics/pathology-only baseline; current survival-free result is recorded in `info/p1_baselines.md`.
- [x] Implement past lesion-volume baseline.
- [x] Implement past log-growth/trend baseline.
- [x] Implement current-image-only readout on the same forecast rows.
- [ ] **BLOCKED — Restrict incident-progression analysis to patients not already PD at the index visit; blocked by finalized prospective endpoint/confirmation definitions.**
- [ ] **BLOCKED — Handle censoring, death, and loss to follow-up explicitly; blocked by finalized outcome/censoring metadata.**
- [ ] **BLOCKED — Compare fixed-horizon prediction with next-appointment prediction; blocked by verified date/horizon metadata.**

**Status:** baseline implementations are complete and an exploratory legacy-cache run is recorded in `info/p1_baselines.md`. The authoritative JEPA comparison is blocked until the Kaggle survival-free retrain and current-schema cache rebuild finish.

**Exit condition:** identify whether the current JEPA state beats simple history and growth baselines on the same prospective task.

## P1 — Reproduce a competitive observed-scan SOTA baseline

- [ ] **BLOCKED — Audit Tikhonov's full protocol, pair inclusion, preprocessing, masks, classifier, and metric aggregation; headline audit is recorded in `info/p1_sota_audit.md`, but full primary-method details are unavailable.**
- [ ] **BLOCKED — Audit TRACE's supplied versus predicted concepts, masks, metadata, and fold protocol; headline audit is recorded in `info/p1_sota_audit.md`, but the full implementation/protocol is unavailable.**
- [ ] **BLOCKED — Verify the reported Amato macro-F1 0.616 result from the primary method; the accessible secondary report does not expose the full protocol.**
- [ ] **BLOCKED — Verify Maurya's balanced-accuracy protocol and hidden-test setup; challenge code and hidden-test access are unavailable.**
- [ ] **BLOCKED — Obtain BraTS progression challenge rules/code/results; no accessible new challenge assignment is available.**
- [ ] **IN PROGRESS — Build a scalar volumetry + nadir + interval CatBoost baseline.** A reduced 22-d sklearn histogram-gradient-boosting proxy is implemented and recorded in `info/p1_sota_audit.md`; true CatBoost/scalar-only features remain.
- [ ] Build a compact shape/intensity/radiomics baseline.
- [ ] Build growth/shrinkage-region radiomics using spatial masks.
- [ ] Build a pretrained paired-image ROI baseline.
- [ ] Use matched tuning budgets and patient-wise nested CV.
- [ ] Record train/validation curves for all image models.
- [ ] **IN PROGRESS — Report minority-class recall, not only macro-F1.** The new tabular baseline emits per-class recall; the complete assessment scorecard still needs the remaining arms.

### Assessment ablation ladder

- [ ] Volumetry and interval only.
- [ ] Shape/intensity/growth-region radiomics.
- [ ] Pretrained paired-image features.
- [ ] Radiomics + pretrained paired-image features.
- [ ] Above + history features.
- [ ] Above + JEPA trajectory state.
- [ ] Late probability fusion using out-of-fold predictions only.

**Go condition:** the best hybrid beats the strongest reproduced baseline by a meaningful paired patient-level margin, then confirms externally.

**Stop condition:** if radiomics wins and JEPA adds nothing, keep the radiomics model and report the negative JEPA result.

## P1 — Test whether temporal dynamics add value

- [x] Ensure assessment features include the observed assessment scan when appropriate; the refreshed observed-pair tabular baseline uses follow-up radiomics/features.
- [x] Compare frozen BRAINIAC current-image features; current-image-only prospective baseline is recorded in `info/p1_baselines.md`.
- [x] Compare mean/history pooling; full result is recorded in `info/p1_baselines.md`.
- [x] Train a last-visit residual MLP; full result is recorded in `info/p1_baselines.md`.
- [x] Train a GRU baseline; full result is recorded in `info/p1_baselines.md`.
- [x] Train the temporal transformer under the same data and tuning budget; the existing A27 temporal task-training comparison is recorded in `info/ablations.md`.
- [x] Compare JEPA temporal training with task-supervised temporal training; existing A27 is the recorded comparison, with refreshed current-schema controls still pending.
- [x] Add scan-count-only and missingness-only controls; the scan-count/observed-visit control is recorded in `info/p1_baselines.md`.
- [x] Run order-shuffled-history control; result is recorded in `info/p1_baselines.md`.
- [x] Run truncated-history control; result is recorded in `info/p1_baselines.md`.
- [ ] Test cumulative-time or time-relative-to-prediction encoding.
- [ ] Check whether the transformer encodes order beyond incoming time gaps.
- [ ] Cross backbone × objective where compute permits; do not attribute a gain to JEPA from a backbone-only comparison.

**Exit condition:** demonstrate an incremental, reproducible gain from history and then from JEPA over matched non-JEPA history models.

## P2 — Build lesion-aware forecasting

- [ ] Define the clinically grounded state `u_t`:
  - [ ] enhancing burden
  - [ ] nonenhancing disease
  - [ ] edema/FLAIR abnormality
  - [ ] resection cavity
  - [ ] shape and spatial extent
  - [ ] prior growth rate
  - [ ] current-to-nadir change
  - [ ] modality availability and segmentation uncertainty
  - [ ] treatment phase and time since intervention
- [ ] Extract volumes/radiomics in physical/common geometry, not resized identity-affine tensors.
- [ ] Preserve registration transforms and voxel-volume metadata.
- [ ] Fit persistence, linear trend, log-growth, site-shrinkage, and scalar gain baselines.
- [ ] Fit a small zero-initialized residual forecast model:
  - [ ] `u_hat(t+h) = u_t + g(H_t,h) * delta(H_t,h)`
  - [ ] condition on explicit horizon and true time gap
  - [ ] keep global BRAINIAC state as a separate branch
- [ ] Evaluate volume/measurement forecast error.
- [ ] Evaluate incident progression risk at fixed horizons.
- [ ] Add a fixed-teacher lesion-feature prediction objective only after deterministic baselines pass.
- [ ] Add uncertainty only after the deterministic model beats persistence.
- [ ] Evaluate proper scores, interval coverage, and sharpness.
- [ ] Evaluate mask forecasting against no-change mask persistence.
- [ ] Report stable versus changing strata without selecting the test set by change status.

**Go condition:** improve clinical lesion outputs and calibrated progression risk on a new site, not merely latent cosine loss.

## P2 — Acquire and audit external supervision

### UCSF-ALPTDG

- [ ] Confirm access and licensing.
- [ ] Inventory usable GBM patients, labels, masks, modalities, and intervals.
- [ ] Check overlap with any preoperative datasets and segmentation-model training data.
- [ ] Audit whether registration/preprocessing uses the second scan.
- [ ] Split patients into pretraining, development, and external test roles before training.
- [ ] Pretrain lesion/change features on permitted training patients.
- [ ] Evaluate measurement/change sensitivity on held-out UCSF patients.
- [ ] Test incremental benefit on the corrected LUMIERE task.

### Burdenko-GBM-Progression

- [ ] Confirm access, labels, treatment-response definitions, and follow-up completeness.
- [ ] Audit registration and scanner/protocol variation.
- [ ] Inventory RT plans, dose, molecular data, and longitudinal masks.
- [ ] Reserve a temporal/external test role before inspecting outcomes.
- [ ] Use for long-trajectory forecasting only after label harmonization.

### UCSD-PTGBM

- [ ] Confirm access and licensing.
- [ ] Inventory tumor-positive versus treatment-change-only cases.
- [ ] Use diffusion/perfusion and expert masks for tumor-versus-treatment-effect auxiliary learning.
- [ ] Keep enriched sampling separate from natural-prevalence calibration.
- [ ] Do not convert its labels into fabricated four-class RANO labels.

### SAILOR

- [ ] Reverify content-filtered inputs and timestamps.
- [ ] Recheck treatment labels and numeric RANO codebook.
- [ ] Keep SAILOR as developmental transfer data, not a pristine final test.
- [ ] Evaluate support-only adaptation separately from zero-shot transfer.

## P2 — Treatment effect and progression mechanism

- [ ] Model observed treatment timing before attempting counterfactuals.
- [ ] Add RT/TMZ timing where independently documented.
- [ ] Add diffusion/perfusion only with matched availability and missingness handling.
- [ ] Separate true progression, pseudoprogression, radiation necrosis, and nonspecific change.
- [ ] Keep pathology-confirmed and clinically adjudicated outcomes separate.
- [ ] Test whether treatment-effect features improve calibration and minority-class recall.
- [ ] Do not claim treatment counterfactual benefit from swapping an action token.

## P3 — External validation and release

- [ ] Freeze model, preprocessing, labels, hyperparameters, and threshold policy.
- [ ] Run one untouched institutional or hidden evaluation.
- [ ] Report zero-shot and support-adapted results separately.
- [ ] Keep transductive CORAL/site alignment clearly separate from pristine transfer.
- [ ] Use fixed query patients and nested support sets for K-shot experiments.
- [ ] Report calibration, uncertainty, missingness, and site-stratified errors.
- [ ] Report negative results and all model-selection decisions.
- [ ] Update the SOTA table only with matched tasks and metrics.
- [ ] Write the final claim as assessment, forecasting, or both—never conflate them.

## Explicitly defer

- [ ] Do not run long champion resumes or larger horizon heads.
- [ ] Do not bundle augmentation, weighting, surgery filtering, and architecture changes in one experiment.
- [ ] Do not build a diffusion/voxel generator before lesion forecasts work.
- [ ] Do not use broad adversarial site alignment before measuring simple gain/mean/covariance corrections.
- [ ] Do not discard explicit measurements because the 22-feature post-hoc concat was null.
- [ ] Do not promote raw surprise to early warning before the corrected AUC analysis.
- [ ] Do not make causal treatment claims from 27 observational SAILOR patients.

## Three-item minimum plan

If resources are limited:

1. [ ] Repair the task, label, cache, and metric contracts.
2. [ ] Reproduce paired-scan radiomics + pretrained-image assessment and test history/JEPA as an incremental feature.
3. [ ] Obtain independent expert lesion-change supervision and evaluate on a new institution.
